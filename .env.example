# Optional: enables the magic wand (AI-suggested steps).
# Get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=

# Optional: override the Gemini model (defaults to gemini-3.8-flash).
GEMINI_MODEL=

# Optional (local dev only): where the SQLite database lives. Defaults to ./data/todos.db.
# DATABASE_PATH=./data/todos.db
